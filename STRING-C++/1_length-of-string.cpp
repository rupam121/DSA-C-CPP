#include <bits\stdc++.h>
using namespace std;

int main()
{
    std::string s = "wellcome";
    // for (int i = 0; s[i] != '\0'; i++)
    // {
    //     printf("length is %d", i);
    // }
    int l = s.length();
    std::cout << "length is: " << l << endl;
    return 0;
}